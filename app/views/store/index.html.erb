<% if notice %>
    <p id="notice"><%= notice%></p>
<% end %>

<% unless @count < 5 %>
    <h4>You've been here <%= pluralize(@count.to_i, "time") %></h4>
<% end %>
<h1>Your BookStore Catalog</h1>

<% "You have been here #{pluralize(@count.to_i, 'time')}" if @count>5 %>

    <% @products.each do |product| %>
    <div class="entry">
      <%= link_to image_tag(product.image_url, border: 0), line_items_path(product_id: product), method: :post%>
      <h3><%= product.title %></h3>
      <%= sanitize(product.description) %>

      <div class="price_line">
        <%= product.book_number %>
        <span class="price"><%= number_to_currency(product.price) %></span>
        <%= button_to 'Add to Cart', line_items_path(product_id: product) %>
      </div>
    </div>
<% end %>
